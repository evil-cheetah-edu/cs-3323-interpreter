(* TODO(eugene): Remove `_` from {input} and {output};
   Currently kept to avoid compilation error due to unused variables
   AND be able to unit test the functions
*)
let interpreter ((_input: string), (_output: string)): unit = ()
